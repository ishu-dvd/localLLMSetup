__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
*.egg-info/
dist/
build/
models/
*.gguf

# Generated by `localllm up` / `join` / `key export`. Every one of these can
# contain a device API key in plain text, and the default output directory for
# all three is the current one - so running them from a checkout, which the
# README explicitly invites, drops secrets into a tracked path.
deploy/
keys.txt
localllm-client.json
.aider.conf.yml
.aider.model.metadata.json
octofriend.json5
opencode.json
settings.json
config.yaml
cline-setup.md
